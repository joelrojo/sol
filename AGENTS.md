# sol — how to work here

> **The mountain is the marketing.**

Every session: read `direction.md` in full before touching anything.

## Rules

- `direction.md` is canon — only Joel-approved direction lives there. Don't change its meaning without his say; tightening language is fine.
- Everything else — proposals, explorations, open items — goes in `seeds.md`, dated. Promote to canon only when Joel confirms.
- Keep the repo lean. Vite app at the root. No second static `site/` tree.
- Personal context: `~/neo` — follow its `AGENTS.md` reading map. Status: `~/neo/work/projects/valle-del-sol.md`.
- Chingón (`~/dev/chingon`) is a sibling, not a template. Don't copy the settle mechanic.

## Git

- Commit directly to `main`, terse messages.
- Remote: `joelrojo/sol`. Pull before working, push after committing.
