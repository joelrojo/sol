# seeds

> Not yet canon. Dated, terse. Promote to `direction.md` only when Joel confirms.

## Open

- **Remaining vision beats** — hard land; ranch / school / retreat apron geometry; close (`The land becomes the institution.` + valle.land). First pass is 1–4 + thesis only.
- **Type** — splash type lives in the still. Vision captions use a system serif until a study lands.
- **Sound** — later. Not on `/`.
- **Domains** — Joel buying valle.land + valle.la. DNS after splash is up.
- **GoatCounter** — after Pages is live, create a site (e.g. valle.goatcounter.com) and drop the snippet in `index.html`. Paths `/` vs `/vision` are the point. Not Counter.dev. No Google.
- **Repo visibility** — public so GitHub Pages works on a free plan (same as chingón). The deck will be shared anyway.

## Decided (2026-09-06)

- Place name: Valle del Sol. Doorway: valle.land + valle.la. Tierra Sol set aside.
- `/` is the still lockup. `/vision` is the first camera-scroll.
- Stack: Vite + React + R3F + GSAP ScrollTrigger.
- Host: GitHub Pages from `dist/` on `joelrojo/sol`.
